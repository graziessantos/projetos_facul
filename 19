#include <stdio.h>
int main (){

    int A, B;

    scanf("%d %d", &A, &B);

    if (A % B == 0 || B % A == 0)
        printf("SAO MUTIPLOS");
    else if (A % B == 1 || B % A == 1)
    {
       printf("Nao sao multiplos");
    }

    return 0;
}